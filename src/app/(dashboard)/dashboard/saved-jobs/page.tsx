"use client";

import React, { useEffect, useState } from "react";

export default function DashboardSavedJob() {

  //  const [job, setJob] = useState<Job| null>(null);
  // useEffect(() => {
  //   const fetchSavedJobs = async () => {
  //     try {
  //       const res = await getSavedJobs("1", "10");
  //      setJob (res.data[0]); // Assuming you want to display the first saved job
  //       console.log("Saved Jobs:", res.data);
  //     } catch (error) {
  //        console.error("Lỗi khi gọi API:", error);
  //     }
  //   };

  //   fetchSavedJobs();
  // }, []);

  return (
    <div>
     <p>hahah</p>
    </div>
  );
}
